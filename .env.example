# Backend configuration
# Substitua <DB_HOST> pelo IP/DNS público da instância (ex.: 192.169.0.189 ou db-postgres)
DATABASE_URL=postgresql://admin:cloudq@<DB_HOST>/meubanco
PORT=4000

# Frontend configuration
VITE_API_URL=http://localhost:4000
